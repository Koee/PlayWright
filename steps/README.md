# Steps

Put reusable business flows here when a spec starts to become long.

Examples:

- `checkout.steps.ts`: select product, proceed to checkout, fill customer info.
- `copy.steps.ts`: prepare QR/copy card, click copy, verify clipboard state.

Specs should read like test scenarios. Repeated flow logic belongs here.
